{
  "name": "gogol-analytics",
  "version": "1.0.0",
  "description": "Web analytics tracking application",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:tracking": "playwright test simple-tracking.spec.js --project=chromium --headed",
    "serve": "php -S localhost:8000 router.php",
    "serve:background": "php -S localhost:8000 router.php &"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  },
  "dependencies": {
    "sqlite3": "^5.1.7"
  }
}
