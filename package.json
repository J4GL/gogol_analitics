{
  "name": "visitor-tracking-dashboard",
  "version": "1.0.0",
  "description": "PHP-based visitor tracking dashboard with real-time analytics",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "install:playwright": "playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.37.0"
  },
  "dependencies": {
    "chart.js": "^4.3.0"
  }
}